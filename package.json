{
  "name": "my-figma-plugin",
  "version": "1.0.0",
  "description": "A Figma plugin to log clicks and generate heatmaps.",
  "main": "modules/main.js",
  "scripts": {
    "build": "npm run clean && npm run copy-files",
    "clean": "rm -rf dist",
    "copy-files": "mkdir -p dist && cp modules/main.js dist/modules/main.js && cp index.html dist/index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "clean-webpack-plugin": "^4.0.0",
    "html-webpack-plugin": "^5.6.0",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  }
}
